const Nonlogged=require('../models/nonlogged');
require('dotenv').config();
const Solution=require('../models/solution');

function addView(id,req){
        
        // return token;
}

module.exports=addView();